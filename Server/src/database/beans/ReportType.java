package database.beans;

/**
 * Enum that represents the different types of report that can be run on the database.
 * @author Charlie
 */
public enum ReportType {
	CONSULTANT, VACANCY, ORGANISATION;
}
